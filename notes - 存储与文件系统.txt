存储与文件系统
	存储
		磁介质（硬盘，磁带），半导体（内存，固态硬盘，闪存卡等），光介质（光盘）等。
	文件系统 FS
		文件系统是用于在存储介质上组织和存取数据的软件。如将卷空间划分为：目录表、文件分配表、数据区等。
		它提供了数据分组的抽象机制（文件，目录等），以及在此抽象机制上的操作接口。文件系统有很多种，以适应不同的底层存储机制（如硬盘，光盘，网络等），达到不同的设计目标（如速度，灵活性，安全等）。
		它通常把存储介质抽象为卷（Volume），即逻辑磁盘，一个卷可能对应一个或多个物理磁盘或磁盘分区。